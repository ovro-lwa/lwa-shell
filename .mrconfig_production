# myrepos config file for ovro-lwa Project

[ovro_data_recorder]
checkout =
    git clone https://github.com/lwa-project/ovro_data_recorder
update =
    git pull --tags origin main
    git checkout -- .
    
[lwa-cfg]
checkout =
    git clone https://github.com/ovro-lwa/lwa-cfg.git
update =
    git pull --tags origin main
    git checkout -- .

[lwa-pyutils]
checkout =
    git clone https://github.com/ovro-lwa/lwa-pyutils.git
update =
    git pull --tags origin main
    git checkout v1.4.5
    git checkout -- .

[lwa-controlscripts]
checkout =
    git clone https://github.com/ovro-lwa/lwa-controlscripts.git
update =
    git pull --tags origin main
    git checkout -- .

[lwa-antpos]
checkout =
    git clone https://github.com/ovro-lwa/lwa-antpos.git
update =
    git pull --tags origin main
    git checkout 0.6.10
    git checkout -- .

[ARXtesting]
checkout =
    git clone https://github.com/ovro-lwa/ARXtesting.git
update =
    git pull --tags origin main
    git checkout -- .

[mnc_python]
checkout =
    git clone https://github.com/ovro-lwa/mnc_python.git
update =
    git pull --tags origin main
    git checkout 0.8.10
    git checkout -- .

[dada2ms]
checkout =
    git clone https://github.com/ovro-lwa/dada2ms.git
update =
    git pull --tags origin master
    git checkout -- .

[caltech-lwa]
checkout =
    git clone https://github.com/realtimeradio/caltech-lwa
    cd caltech-lwa
    git submodule init control_sw/libs/casperfpga
    git submodule update
update =
    git pull --tags origin master
    git checkout v2.0.22
    git submodule update
    git checkout -- .

[caltech-bifrost-dsp]
checkout = 
    git clone https://github.com/realtimeradio/caltech-bifrost-dsp.git
    cd caltech-bifrost-dsp
    git submodule init
    git submodule update
update =
    git pull --tags origin main
    git checkout v1.0.8
    git submodule update
    git checkout -- .

[bifrost]
checkout =
    git clone https://github.com/ledatelescope/bifrost
update =
    git pull --tags origin master
    git checkout -- .

[dsa110-pyutils]
checkout =
    git clone https://github.com/dsa110/dsa110-pyutils.git
update =
    git pull --tags origin master
    git checkout v3.8.1
    git checkout -- .

[swdeploy]
checkout =
    git clone https://github.com/ovrocaltech/swdeploy.git
update =
    git pull --tags origin main
    git checkout v1.5.4
    git checkout -- .

[deploy-test]
checkout =
    git clone https://github.com/ovrocaltech/deploy-test.git
update =
    git pull --tags origin main
    git checkout v1.2.1
    git checkout -- .

[ovro_data_recorder]
checkout =
    git clone https://github.com/lwa-project/ovro_data_recorder.git
update =
    git pull --tags origin main
    git checkout -- .
